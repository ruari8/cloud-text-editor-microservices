[package]
name = "average-word-length"
version = "0.1.0"
edition = "2021"

[lib]
name = "average_word_length"
path = "src/lib.rs"

[dependencies]
actix-web = "4.0"
actix-cors = "0.6"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"

[dev-dependencies]
actix-rt = "2.7"
